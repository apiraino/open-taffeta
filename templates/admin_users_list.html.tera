<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
  </head>
  <body>
    <h1 style="text-align:center;">{{ title }}</h1>

    <div>
      {% for user_role in users %}
      <span>
        <a href="/user/{{ user_role.0.id }}">{{ user_role.0.email }}</a> ({{ user_role.1.name }}):
        <input type="checkbox" name="is_active" checked={{ user_role.0.is_active }} />
      </span><br />
      {% endfor %}
    </div>

  </body>
</html>
